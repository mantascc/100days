export const meta = {
    slug: '15-binary-camera-grid',
    title: 'Binary Camera Grid',
    warmupMs: 1500,
    disabled: true,
};

export async function play(page) {
    await page.waitForTimeout(3500);
}
