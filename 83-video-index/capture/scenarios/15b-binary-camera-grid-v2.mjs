export const meta = {
    slug: '15b-binary-camera-grid-v2',
    title: 'Binary Camera Grid v2',
    warmupMs: 1500,
    disabled: true,
};

export async function play(page) {
    await page.waitForTimeout(3500);
}
