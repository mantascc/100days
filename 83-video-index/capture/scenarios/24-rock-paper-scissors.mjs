export const meta = {
    slug: '24-rock-paper-scissors',
    title: 'Rock Paper Scissors',
    warmupMs: 600,
    zoom: 1,
};

export async function play(page) {
    await page.waitForTimeout(3600);
}
