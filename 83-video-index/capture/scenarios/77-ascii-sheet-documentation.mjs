export const meta = {
    slug: '77-ascii-sheet-documentation',
    title: 'Ascii Sheet Documentation',
    warmupMs: 800,
};

export async function play(page) {
    await page.waitForTimeout(3500);
}
