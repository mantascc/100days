export const meta = {
    slug: '40-cube-grid-rules',
    title: 'Cube Grid Rules',
    warmupMs: 1500,
    disabled: true,
};

export async function play(page) {
    await page.waitForTimeout(3500);
}
