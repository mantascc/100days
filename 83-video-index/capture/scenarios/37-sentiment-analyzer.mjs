export const meta = {
    slug: '37-sentiment-analyzer',
    title: 'Sentiment Analyzer',
    warmupMs: 1500,
    disabled: true,
};

export async function play(page) {
    await page.waitForTimeout(3500);
}
