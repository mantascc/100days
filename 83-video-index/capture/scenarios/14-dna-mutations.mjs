export const meta = {
    slug: '14-dna-mutations',
    title: 'DNA Mutations',
    warmupMs: 1200,
    zoom: 1,
    crop: '720:720:0:0',
};

export async function play(page) {
    await page.waitForTimeout(3500);
}
