---
id: instances/aida
type: instance
title: Aida
status: provisional
version: 0.1
core: computational-minimalism
accent: cph-yellow (#ffaa00)
provenance: aida-01-phase-portrait
tags: [warmth, oscilloscope, provisional]
---

# What
The youngest, least-settled release: a single warm-yellow identity built
around oscilloscope warmth. Aida is barely an instance yet — one accent,
one sketch, an itch toward a phosphor-glow phase-portrait aesthetic. It is
`provisional` on purpose: it has not recurred, so it has not earned
permanence. If a second sketch reaches for the same warmth, it graduates.

# Overrides
A minimal fork — a single named accent over the core dark ground. Nothing
else is committed yet.

| Axis | Override |
|------|----------|
| Accent | `cph-yellow #ffaa00` (Copenhagen, oscilloscope warmth) |
| Character | phosphor / phase-portrait glow on void ground |
| Everything else | inherited; undecided pending a second sketch |

# Provenance
Aida 01 phase portrait — the source of the `cph-yellow` named color, marked
`provisional` in [tokens/color](../tokens/color.md). Permanence here is
pending recurrence: one more sketch in this register promotes both the
instance (0.1 → 1.0) and the color (provisional → active).
