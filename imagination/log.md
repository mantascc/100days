# Imagination — Log

## 2026-08-09
* **Format — coverage tracking (SPEC §8.1, §7.1.1).** The layer could not report what it had read. Coverage is *not* derivable from `spawned:` — §7.1 leaves a once-seen signal as a note, so a correctly-inhaled sketch can produce zero entities and look identical to one never opened. Added [`coverage.md`](coverage.md), a ledger with one row per sketch, generated by [`coverage.py`](coverage.py). The script owns the row set; `/inhale` owns the outcome column.
* **New outcome — `no-signal`.** A sketch that yields nothing is now recorded as read-with-nothing-found rather than being mined for an invented entity. Per new §7.1.1 it closes the *read*, not the sketch: a later inhale may attach it retroactively to a new entity's `spawned` when it turns out to have been an earlier sighting. Same reversibility §6 gives `spent`.
* **Fix — promotion double-counting.** Six daily sketches were promoted to indexed pieces (`sketch-eixample`→`88`, `sketch-agent-stream-glyphs`→`85`, `sketch-agent-stream-scrub`→`86`, plus `62`/`65`/`66`). Harvesting both halves would enter one piece of work into `spawned:` twice and inflate every affected Thread's productivity trace — which `graph.json` and the barcode snapshots also read. Now marked `covered-by`; only the indexed piece is harvested.
* **`/inhale` rewritten.** No-argument runs now harvest *everything* pending, but in clusters — the promotion rule assumes signals accrue over time, so a thirty-sketch single pass would push every recurring motif over the "twice earns a Thread" line at once. One cluster per diff, ledger updated after each.
* **Spark + two sketches, both `no-signal`.** `/spark` run; the maker took the **Collide** ([`sketch-torus-sheet`](../daily-sketch/sketch-torus-sheet/)) and the **Press** ([`sketch-tube-driven`](../daily-sketch/sketch-tube-driven/)). Both were built and both work — the torus closes byte-for-byte, and the tube's `bypass` control proves the picture never reacts. Neither lifted anything into the layer. The maker's read: *"feels like a cold remix of ideas."* Recorded as `no-signal` rather than mined for entities, which is the first real use of the outcome.
* **Note (once, so not promoted).** Both flat sketches came from the layer's own recombination strategies. A `/spark` prompt can be structurally correct — genuinely distant entities, a real open theme — and still produce work with no pull, because distance between entities is not the same as desire. If this recurs it is a Theme about the exhale itself. Once is a note.
* **Observation — no dormant high-charge entities.** All six dormant entries are Seeds at medium or low, so `/spark`'s **Revive** had nothing to surface and fell back to the best available. The §7.3 aging rule has had nothing to bite on yet.
* **Ghost seed caught by coverage.** [`seeds/apophenia-arc-instrument`](seeds/apophenia-arc-instrument.md) is `active` / `charge: high` / `spawned: []`, but `daily-sketch/sketch-apophenia-arc` already implements all four phases, the `valveOpen` boolean and the coin-flip load. The layer did not know because that sketch is unharvested, so `/spark` was ranking a built idea as its highest-charge unmade Seed. Left as-is pending the backlog inhale; should become `spent`.
* **Baseline coverage.** Indexed **85/93 harvested**, 1 blocked (`63-constellation-map`, no `interface.md`), 7 pending. Daily sketches **1/40 harvested**, 6 covered-by, 8 blocked, 25 pending. The backlog is everything from `85-` onward except `91`, plus the daily-sketch folder, which the layer has effectively never read. Largest pending cluster: 5 agent-stream sketches with no thread between them.

## 2026-08-01
* **Inhale** — `91-retro-primitives` (promoted from `daily-sketch/sketch-retro-primitives`). Created technique [crt-tube-treatment](techniques/crt-tube-treatment.md) — the bloom/grain/scanlines/tube pipeline that finally reached the reference after a first-pass "pinball" version. Strengthened threads [design-system-and-reference-sheets](threads/design-system-and-reference-sheets.md) (palette-swap family) and [grids-as-compositional-frame](threads/grids-as-compositional-frame.md) (2×2 tube-cell grid); added the sketch to [themes/showing-mechanism-vs-polish](themes/showing-mechanism-vs-polish.md) as a decisive polish datapoint.

## 2026-06-14
* **Initialization** — Established the Imagination layer ([SPEC.md](SPEC.md) v0.1): six type folders, `/spark` and `/inhale` rituals.
* **Harvest** — Retroactive sweep of the existing sketchbook seeded 44 entities: 11 threads, 13 techniques, 6 themes, 9 sources, 5 dormant seeds. `collisions/` left empty — generated forward via `/spark`.
* **Note** — `80-network` could not be read during the harvest; its membership in `threads/networks-and-graphs` and `techniques/proximity-link-drawing` was inferred from the folder name. Confirm on next `/inhale`.
* **Inhale (coverage pass)** — Folded the 7 remaining indexed sketches (`26`, `35`, `36`, `37`, `61`, `62`, `71`) into the layer; wrote `interface.md` for the three that had none (`61`, `62`, `71`). Coverage now **84/84** indexed sketches. Added 7 entities: thread `language-and-nlp-interpretability`; techniques `geodesic-great-circle-path`, `typewriter-jitter-output`, `per-cell-threshold-crystallization`, `scripted-await-timeline`, `drag-reorder-collision-packing`; seed `computed-not-curated-thumbnails`. Layer total: 51 entities.
* **Finding** — `36-spinning-globe` and `37-sentiment-analyzer` are the same React/transformers.js sentiment app; the `36` folder name is stale (no globe). Both credited to `threads/language-and-nlp-interpretability`.
* **Spark + collision** — `/spark` run; maker took the **Collide**. Logged first `collisions/` entity: [`vicsek-flocking-x-typography`](collisions/vicsek-flocking-x-typography.md) (`sources/vicsek-flocking` × `threads/generative-typography-and-text`, charge high, state active). `collisions/` no longer empty. Layer total: 52 entities.

## 2026-06-17
* **Rename** — Entity types renamed across the layer: Vein→Thread, Move→Technique, Tension→Theme (folders `veins/`→`threads/`, `moves/`→`techniques/`, `tensions/`→`themes/`). Updated SPEC, README, rituals, `/spark` + `/inhale` commands, all entity files, the snapshot generator, index, and graph.json. Source / Seed / Collision unchanged.

## 2026-06-16
* **Hyperfocus** — `seeds/computed-not-curated-thumbnails` promoted `dormant`→`active`, charge `medium`→`high`. Body expanded into four branches (Fingerprint / Scale / Reach / Generalise) and an open-questions section. Added `feeds:` links to `themes/randomness-feeling-intentional` and `themes/searching-vs-mapping`. Legacy three-prompt list retained as a footer cross-reference. Layer total unchanged at 52 entities.
