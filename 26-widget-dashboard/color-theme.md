Theme from #151621:
Background layers (elevation):

Base: #151621 (canvas)
Surface 1: #1f2031 (collapsed widgets)
Surface 2: #292a3e (expanded widgets)
Surface 3: #33344b (modal overlay)
Overlay dim: rgba(21, 22, 33, 0.85) (modal background)

Text:

Primary: #f5f5f7 (titles, main text)
Secondary: #a8aab8 (labels, metadata)
Tertiary: #6b6d7f (hints, disabled)

Borders/dividers:

Subtle: #23253a
Default: #2f3041
Strong: #3f4058

Logic:
Each elevation level is ~10 lightness units higher. Text uses desaturated versions of the base with high contrast.